{% extends 'baseadmin.html.twig' %}

 
 


 {% block body %}

   
 
    <!-- ======= Contact Section ======= -->
               
 

  {# <section id="main-content"> #}
      {# <section class="wrapper"> #}

           <div class="row mt">
         <div class="container-fluid">

    
   	   {% for message in app.flashes('errorData') %}
          <div class="alert alert-danger" role="alert">
             {{message}}
          </div>
        {% endfor %}

 
        {% for message in app.flashes('errorSaisie') %}
          <div class="alert alert-danger" role="alert">
            {{message}}
          </div>
        {% endfor %}
       
    
         {% for message in app.flashes('Success') %}
          <div class="alert alert-success" role="alert">
             {{message}}
          </div>
        {% endfor %}
     

 

      {{form_start(form,{'attr':{'style':'margin-bottom:3em;margin-top:3em'}})}}

    
        <div class="form-group">
            <label for="exampleInputEmail1">Pseudo <strong style="color:red">*</strong></label>
             {{form_widget(form.username,{"attr": {"class":"form-control","placeholder":"Entrer le Pseudo de votre Nouveau personnel"}})}}
            <small id="username" style="color:ligthgrey">tout les champa suivi de l'etoile rouge sont obligatoire.</small>
        </div>
        <div class="form-group">
            <label for="exampleInputPassword1">Password<strong style="color:red">*</strong></label>
              {{form_widget(form.Password,{"attr": {"class":"form-control","placeholder":" Saisire le Mot de pass"}})}}
               <small id="password" style="color:ligthgrey"></small>
                   
        </div>
       
        <div class="form-group">
                <label for="exampleFormControlTextarea1">Comfirmer le Password<strong style="color:red">*</strong></label>
                   <input type="password" class="form-control" id="ComfirmPassword" placeholder="Confirmer le Mot de Pass" required>
                  <small id="Comfirm" style="color:ligthgrey"></small>
                
        </div>
        
           {{form_widget(form.Inscris ,{"attr": {"style":"width:100%","class":"btn btn-primary"}})}}
        

   {{form_end(form)}}

 </div>
      </div>
  
  
    
    <!-- End Contact Section -->
 <script src="{{asset('js/UpdateAnnonce.js')}}">
 
</script>
  {% endblock %}

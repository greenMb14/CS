
{% for item in data %}

        {% if item.id > id %}


         {% if item.iduser == 0 %}
            
          <div id="tchatcontent">
                     <div id="tchatinfo">
                          <div id="tchatname">
                               <i>{{item.email}}</i>
                               
                          </div>
                          <div id="tchatheur">
                                  <i>15:40:01</i> 
                          </div>
                       </div>
                <div id="sms2">
            {{item.message}}
              <i id="{{item.id}}"></i>
                </div>
          </div>

          <br>
<input type="hidden" value="{{item.email}}">        
        {% endif %}

    {% endif %}
 
{% endfor %}
